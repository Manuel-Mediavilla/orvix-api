{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "php artisan migrate --force && php artisan config:clear && php artisan cache:clear",
    "healthcheckPath": "/up",
    "restartPolicyType": "ON_FAILURE"
  }
}
